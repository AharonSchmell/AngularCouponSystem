/* An enum used throughout the project to sort a coupon list according to different filters */
export enum CouponsFilter {
    MOST_SOLD,
    ABC,
    START_DATE,
    END_DATE,
    PRICE_ASCENDING,
    PRICE_DESCENDING
}